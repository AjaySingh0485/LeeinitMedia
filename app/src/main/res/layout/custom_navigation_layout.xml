<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <TextView
        android:id="@+id/textName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/marginPadding20"
        android:layout_marginTop="@dimen/marginPadding15"
        android:layout_marginEnd="@dimen/marginPadding20"
        android:fontFamily="@font/scan_regular"
        android:gravity="center_vertical"
        android:padding="@dimen/marginPadding5"
        android:text="@string/M_NAME"
        android:textColor="@android:color/black"
        android:textSize="@dimen/text25"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/viewNavigation"
        android:layout_width="match_parent"
        android:layout_height="@dimen/marginPadding2"
        android:layout_marginStart="@dimen/viewHeightWidth50"
        android:layout_marginTop="@dimen/marginPadding15"
        android:layout_marginEnd="@dimen/viewHeightWidth50"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/textName" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycleNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/marginPadding15"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/viewNavigation" />

    <View
        android:id="@+id/viewNavigation1"
        android:layout_width="match_parent"
        android:layout_height="@dimen/marginPadding2"
        android:layout_marginStart="@dimen/viewHeightWidth50"
        android:layout_marginTop="@dimen/marginPadding15"
        android:layout_marginEnd="@dimen/viewHeightWidth50"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/recycleNavigation" />
</androidx.constraintlayout.widget.ConstraintLayout>
